# COMET Flowchart Translator

A Python pipeline that converts `.rap` Raptor flowchart files to:

- `.json` ASTs
- `.rapcode` DSL
- Mermaid flowcharts
- Control Flow Graph (CFG) images

## Usage

```bash
python run_pipeline.py samples/factorial.rap
